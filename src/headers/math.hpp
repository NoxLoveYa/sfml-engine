#pragma once

namespace engine
{
    namespace math
    {
        //maxcros
        #define PI 3.14159265358979323846

        #define lerp(a, b, t) (a + t * (b - a))
        #define invlerp(a, b, v) ((v - a) / (b - a))

        //functions
    };
}